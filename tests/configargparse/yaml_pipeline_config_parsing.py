"""Tests of the YAML config parser with hierarchy and pipelining."""

# TODO Given an example case, create a yaml config w/ hierarchy and generate
# the configargparser for that yaml file then test it to see if it has all the
# attributes it should w/ the correct values.
